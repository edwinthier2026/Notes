import { Orbit } from 'lucide-react';
import ProgramPlaceholder from './ui/ProgramPlaceholder';

export default function AbonnementenPage() {
  return <ProgramPlaceholder title="Abonnementen" icon={Orbit} />;
}
