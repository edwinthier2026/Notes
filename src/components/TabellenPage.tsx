import { TableProperties } from 'lucide-react';
import ProgramPlaceholder from './ui/ProgramPlaceholder';

export default function TabellenPage() {
  return (
    <ProgramPlaceholder
      title="Tabellen"
      message="De layout van Tabellen werken we hierna verder uit."
      icon={TableProperties}
    />
  );
}
